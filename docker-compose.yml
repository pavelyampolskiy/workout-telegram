services:
  bot:
    build: .
    image: workout_bot:latest
    restart: unless-stopped
    env_file: .env
    environment:
      - DB_PATH=/data/workouts.db
    volumes:
      - bot_data:/data

volumes:
  bot_data: {}
